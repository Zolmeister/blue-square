TODO:
// dont spin drains
// create the rest of the drain shapes
//set shape colors
//position drains along the edges (wthin bounds)
//smart spawner (random location not touching a drain)
//lose a life if drag to the wrong drain
//lose a life if shape not drained in time (and remove shape)
//inscrease spawn rate per level
//smart spawning of shapes based on drains available
//add score visiblity object
//add game over

Animations:
score object animation
//spin shapes faster as their times increase
//shapes break down as death animation
//hearts break down as death animation
//drains glow/animate as collision animation (maybe spin once quickly?)
//game over animation, everything breaks down

Also:
particle effects...

And:
draw tutorial arrow pointing from first spawn (always center) to drain. (remove after level?)
